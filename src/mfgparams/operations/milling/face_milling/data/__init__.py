"""Bundled default face-mill tools TOML package data (non-code).

Mirrors ``operations/drilling/data/__init__.py``: this ``__init__.py``
exists only so ``importlib.resources.files()`` can resolve this directory
as a regular package under Python 3.9/3.10 (which do not reliably support
namespace-package resource resolution); it declares no public API of its
own.
"""
